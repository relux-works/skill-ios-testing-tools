# TASK-260615-184g4c Review 2

Verdict: to-dev.

## Passed

- `swift test` passed: 38 tests in 4 suites. Log: `.temp/TASK-260615-184g4c/swift-test-review-01.log`.
- `git diff --check` passed. Log: `.temp/TASK-260615-184g4c/git-diff-check-review-01.log`.
- `task-board validate` passed. Log: `.temp/TASK-260615-184g4c/task-board-validate-review-01.log`.
- Product-name scan excluding `.temp/**` and `.task-board/**` is empty for `Tap2Cash`, `Tap2CashDemo`, `tap2cash`, `T2C`, and `x-platform-airdrop`. Log: `.temp/TASK-260615-184g4c/product-name-scan-review-01.log`.
- Tap2Cash dry-run evidence exists and shows peer-specific coordinator URLs:
  - `peer-a`: `ws://169.254.14.34:8123/e2e/session`
  - `peer-b`: `ws://169.254.228.136:8123/e2e/session`
  - Log: `.temp/TASK-260615-184g4c/tap2cash-dry-run-usb-02.log`.

## Blocking Finding

The cleanup removed explicit Tap2Cash names from several files, but toolkit-owned skill docs still carry consumer-flow wording and sender/receiver examples. The repo-level Project-Neutral Toolkit Rule covers skill docs, and this task checklist explicitly includes neutralizing Tap2Cash names in toolkit-owned help text and skill docs.

Evidence:

- `agents/skills/ios-testing-tools/SKILL.md:397` still uses `sender/receiver`, nearby discovery, and BLE advertisement/scanning as the default paired-device example.
- `agents/skills/ios-testing-tools/references/physical-ios-ios-e2e-sync.md:3`, `:17-18`, `:39-50`, `:63-66`, `:74-85`, `:129-133`, and `:173-178` still use receiver/sender role names, `APP_AUTOMATION_RECEIVER_MODE`, receiver launch/log/artifact names, and `switch_to_send_mode` / `switch_to_receive_mode` examples.
- `agents/skills/ios-testing-tools/references/physical-ios-ios-runner-marker-sync.md:129` and `:131` still use `sender_peer_detected_*` screenshot names even though the marker example was neutralized to `peer_beta_peer_detected`.

Required rework:

- Replace residual sender/receiver/reclamation/BLE/nearby consumer-flow examples in toolkit-owned skill docs with neutral placeholders such as `peer alpha`, `peer beta`, `observing peer`, `controlled peer`, `project-specific mode`, and `project-specific event`.
- Rerun the project-neutral static leak scan excluding `.temp/**` and `.task-board/**`.
- Rerun `swift test`; refresh Tap2Cash dry-run evidence if any runner/help behavior changes.
