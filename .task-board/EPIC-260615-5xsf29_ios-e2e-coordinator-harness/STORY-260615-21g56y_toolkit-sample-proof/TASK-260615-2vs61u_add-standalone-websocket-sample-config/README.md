# TASK-260615-2vs61u: add-standalone-websocket-sample-config

## Description
Add sample config under the toolkit repo for a local WebSocket E2E coordinator session with alpha, beta, and observer fake peers.

## Scope
Add neutral sample config in the toolkit repo that declares local coordinator settings, fake peers, peer roles, event expectations, delivery defaults, and artifact output.

## Acceptance Criteria
Config declares coordinator host/port, session defaults, three fake peers, peer names/roles, sample event expectations, delivery defaults, artifact directory, and contains no product-project paths or domain semantics.
